---
const { class: className, id } = Astro.props;
---

<section
  id={id}
  data-section={id}
  class={`section ${className} w-full mx-auto container scroll-m-20 lg:max-w-4xl md:max-w-2xl `}
>
  <slot />
</section>
